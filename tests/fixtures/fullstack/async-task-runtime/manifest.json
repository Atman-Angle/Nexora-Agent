{
  "schemaVersion": "1",
  "id": "async-task-runtime",
  "title": "async task feature reusing Nexora Runtime",
  "category": "async-task",
  "difficulty": "intermediate",
  "projectType": "typescript-single",
  "stack": ["TypeScript", "JavaScript"],
  "requirement": {
    "objective": "Wire note creation as a Nexora Runtime task: create→run→status→artifact (reusing Task/Run/State Machine)",
    "userValue": "Async note creation is recoverable and observable via Runtime status",
    "actors": ["user"],
    "userFlows": [{ "name": "async create", "actor": "user", "steps": ["create note via service", "verify persisted"] }],
    "functionalRequirements": ["create persists via service", "list returns persisted notes"],
    "nonFunctionalRequirements": ["reuse Runtime state machine", "no second state system"],
    "acceptanceCriteria": [
      { "id": "ac1", "description": "create persists a note", "layer": "service" },
      { "id": "ac2", "description": "list returns the created note", "layer": "e2e" }
    ],
    "constraints": ["do not change repository or test", "reuse Nexora Runtime, do not create a second state machine"],
    "assumptions": ["repository is correct"],
    "outOfScope": ["auth", "ui"],
    "compatibilityRequirements": ["additive"],
    "securityRequirements": []
  },
  "setupCommands": [],
  "baselineCommands": [],
  "acceptanceCommands": [{ "command": "node", "args": ["test.js"], "expectedExitCode": 0, "purpose": "acceptance", "timeoutMs": 10000 }],
  "e2eCommands": [{ "command": "node", "args": ["test.js"], "expectedExitCode": 0, "purpose": "e2e", "timeoutMs": 10000 }],
  "regressionCommands": [],
  "allowedPaths": ["src/note/service.js"],
  "forbiddenPaths": ["src/note/repository.js", "test.js"],
  "expectedLayers": ["data", "service", "runtime", "api", "client"],
  "requiredEvidence": ["requirement", "architecture", "contract", "plan", "e2e"],
  "requiresRuntimeReuse": true,
  "scoring": {
    "weights": { "functionalCompleteness": 0.2, "contractConsistency": 0.15, "architectureFit": 0.1, "dataSafety": 0.15, "verificationQuality": 0.1, "scopePrecision": 0.1, "runtimeReuse": 0.1, "runtimeReliability": 0.1 },
    "passThreshold": 0.6
  },
  "timeoutMs": 60000
}
