{
  "schemaVersion": "1",
  "id": "python-single-file",
  "title": "python add implemented as subtract",
  "category": "single-file-logic",
  "difficulty": "basic",
  "projectType": "python-backend",
  "languages": ["Python"],
  "frameworks": [],
  "issue": {
    "objective": "Fix add(a,b) so it returns a+b instead of a-b",
    "reportedSymptoms": ["add(2,3) returns -1 instead of 5"],
    "expectedBehavior": "add(2,3) returns 5",
    "reproductionHints": ["python test.py"],
    "constraints": ["do not change test.py"],
    "acceptanceCriteria": ["python test.py exits 0"],
    "forbiddenChanges": ["modifying test.py"]
  },
  "reproductionCommands": [
    { "command": "python", "args": ["-B", "test.py"], "cwd": ".", "expectedExitCode": 1, "purpose": "reproduce", "timeoutMs": 15000 }
  ],
  "acceptanceCommands": [
    { "command": "python", "args": ["-B", "test.py"], "cwd": ".", "expectedExitCode": 0, "purpose": "acceptance", "timeoutMs": 15000 }
  ],
  "regressionCommands": [
    { "command": "python", "args": ["-B", "-c", "import sys; sys.path.insert(0,'.'); from src.math import add; exit(0 if add(10,5)==15 else 1)"], "cwd": ".", "expectedExitCode": 0, "purpose": "regression", "timeoutMs": 15000 }
  ],
  "allowedPaths": ["src/math.py"],
  "forbiddenPaths": ["test.py"],
  "expectedChangedFiles": ["src/math.py"],
  "forbiddenChangedFiles": ["test.py"],
  "requiredEvidence": ["reproduction-failure", "acceptance-pass"],
  "scoring": {
    "weights": { "functionalCorrectness": 0.3, "regressionSafety": 0.2, "scopePrecision": 0.2, "rootCauseQuality": 0.1, "evidenceQuality": 0.1, "runtimeReliability": 0.1 },
    "passThreshold": 0.6
  },
  "timeoutMs": 60000,
  "staticProvable": false
}
